<?xml version="1.0"?>
<psalm
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	errorLevel="4"
	resolveFromConfigFile="true"
	xmlns="https://getpsalm.org/schema/config"
	xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
	<projectFiles>
		<directory name="src"/>
		<ignoreFiles>
			<directory name="vendor"/>
			<directory name="tests/assets/snapshots"/>
			<directory name="tests/tmp"/>
		</ignoreFiles>
	</projectFiles>
	<issueHandlers>
		<!-- Public API methods not called within the library itself (consumed externally) -->
		<PossiblyUnusedMethod errorLevel="suppress"/>
		<!-- Fluent builder methods whose return values are chained by callers externally -->
		<PossiblyUnusedReturnValue errorLevel="suppress"/>
		<!-- Public API classes and properties used externally, not within the library -->
		<UnusedClass errorLevel="suppress"/>
		<PossiblyUnusedParam errorLevel="suppress"/>
		<PossiblyUnusedProperty errorLevel="suppress"/>
		<!-- Initial null assignments for typed variables are a valid pattern; suppress false positives -->
		<UnusedVariable errorLevel="suppress"/>
	</issueHandlers>
	<stubs>
		<file name="stubs/Override.phpstub"/>
	</stubs>
</psalm>
