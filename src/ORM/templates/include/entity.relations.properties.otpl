<%loop($.relations.list : $relation){%>
<%if($relation.type === 'one-to-one' || $relation.type === 'many-to-one'){%>
		/**
		 * @var \<%$relation.target.class.use_entity%>
		 */
		protected $r_<%$relation.name%>;
<%}%>
<%}%>